# blog
blog by laravel

# Usage
```
> git clone git@github.com:defsky/blog.git
> cd blog
> cp .env.example .env
... edit .env to config database connection
> php artisan migrate
> php artisan db:seed
> php -S 0.0.0.0:1024
open http://localhost:1024/ with browser
```
